{
  "name": "osprey-website",
  "version": "1.0.0",
  "description": "Official website for the Osprey Programming Language",
  "main": "index.js",
  "scripts": {
    "start": "npm run copy-spec && npm run generate-docs && eleventy --serve",
    "dev": "npm run copy-spec && npm run generate-docs && eleventy --serve --watch",
    "build": "npm run generate-docs && eleventy",
    "copy-spec": "node scripts/copy-spec.js",
    "generate-docs": "./scripts/generate-docs.sh",
    "clean": "rm -rf _site",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["osprey", "programming-language", "website", "eleventy"],
  "author": "Christian Findlay",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.1",
    "luxon": "^3.6.1",
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^9.2.0"
  }
}
